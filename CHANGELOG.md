# 更新日志

[English](CHANGELOG.en.md)

本项目遵循「以版本号区分」的格式，每个版本记录新增 / 修复 / 改进。

## 1.6（2026-08-26）

### 新增
- **多语言支持**：新增简体中文 / English / Français / 日本語 四个语种，设置中可切换（默认跟随系统），重启后生效
- **三端独立语言设置**：iOS / macOS / Android 各自保存语言偏好
- **检查更新**：设置页手动检查 GitHub 最新 release，展示版本号与更新说明，可跳转下载页

### 修复
- **Android 通知栏上下换台无反应**
- **Android 播放页标星状态不刷新**
- **macOS App 图标尺寸警告**

### 改进
- **文档多语化**：README / CHANGELOG / 各平台 README 新增英文版

## 1.5（2026-08-26）

### 新增
- **macOS 菜单栏托盘**：左键切换窗口显隐，右键弹出功能菜单。
- **macOS 应用菜单栏**：「文件」「播放」「帮助」完整菜单。
- **macOS 定时停播菜单**：改用原生 Menu 显示全部档位 + 自定义 + 取消定时。

### 修复
- **macOS 导入无预览确认**
- **macOS 帮助菜单「未找到帮助」**：替换默认帮助菜单为打开应用内帮助页。
- **macOS 关闭提示「记住我的选择」默认未勾选**
- **其他一些 UI 和表述问题**：

### 改进
- **展示名统一**：所有用户可见名称统一为「网络电台」。
- **m3u 解析更稳健**：所有 `#` 开头的非 `#EXTINF` 行一律跳过。
- **macOS 关闭窗口行为**：新增「每次询问」模式，弹窗可选「最小化到托盘」或「退出」，支持记住选择。

### 已知问题
- 使用「万能签」安装 iOS 版会导致 m3u 文件导入无反应，建议使用订阅在线列表功能替代；Xcode 正规安装不受影响。

## 1.4（2026-08-21）

### 新增
- **双端播放质量指示器**（macOS「正在播放」栏 + iOS 播放页）：显示编码格式（MP3 / AAC / Opus 等）、码率（kbps）、采样率（kHz）、声道数（单声道 / 立体声 / n 声道）。
- **启动/缓冲延迟**：从发起播放到流就绪可播的耗时（毫秒），按绿（<150）/ 黄（150-299）/ 橙（300-599）/ 红（≥600）四色标注。
- **质量来源多级兜底**：macOS 依次取 ICY 响应头 → Icecast status-json → Qt 元数据；iOS 取实际播放音轨格式描述 → 静态音轨 → 访问日志码率。
- **质量说明悬浮提示**：macOS 鼠标悬浮、iOS 点击问号，提示「质量由电台来源和网络环境决定」。
- **macOS 节目信息变化通知**：换台之外，节目/曲目变化时（有节目信息才发）推送系统通知，大字=艺术家-节目名、小字=台名；同一文本不重复推送。

### 修复
- **iOS m3u 文件导入无反应**：`fileImporter` 在 iOS 17 真机、NavigationSplitView 内层选中文件后回调不触发，改为原生 `UIDocumentPickerViewController`（允许任意类型文件，解析失败明确弹错）。
- 已知问题：使用“万能签”安装本程序会导致上述导入问题出现，建议通过电脑python启动http服务器，提供m3u文件，然后使用订阅在线列表功能“曲线救国”。使用Xcode正规安装的不受影响。关于启动http服务器，请咨询ai助手。

### 改进
- **iOS 关于页版本号**：改为从 Info.plist 动态读取，不再硬编码，避免发版遗漏。

## 1.3

- iOS 定时停播按钮移至标星旁并列；停播后保留电台可重开播。
- 双端新增定时停播功能（可选时长 + 自定义 + 倒计时显示）。

## 1.2

- 新增 iOS 未签名 ipa 打包脚本 `build_ios.sh`。